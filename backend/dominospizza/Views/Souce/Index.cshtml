@model List<Product>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">

        @foreach (var item in Model)
        {
            <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                <a href="">
                    <div class="pizza-item">
                        <div class="pizza-item-img">
                            <img src="img/sous/@item.Image" alt="">
                        </div>
                        <div class="pizza-item-text">
                            <div class="text-head">
                                <h4>@item.Name</h4>
                            </div>
                            <br>

                            <div class="price">
                                <h6>@item.Price$</h6>
                                <div data-id="@item.Id" class="btn btn-lg btn-outline-danger addToCart">
                                    SƏBƏTƏ ƏLAVƏ ET
                                </div>
                            </div>
                        </div>
                    </div>
                </a>
            </div>
        }
    </div>
</div>